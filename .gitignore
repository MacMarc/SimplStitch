# Xcode
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (falls je genutzt)
Pods/

# macOS
.DS_Store

# Python-Runtime (wird per Scripts/bundle_python.sh gebündelt statt eingecheckt)
/PythonRuntime/
/.build-cache/
__pycache__/
*.pyc

# Notarisierung / lokale Signierartefakte
*.p12
*.mobileprovision

# Lokale Test-Assets (nicht Teil des Repos, z.B. manuelle SVG-Importtests) —
# alle .svg-Dateien direkt im Repo-Root, nicht rekursiv (echte App-Assets liegen
# unterhalb von SimplStitch/, nicht im Root)
/*.svg
